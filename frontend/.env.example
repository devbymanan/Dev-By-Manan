# Copy to .env.local for local development

NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# reCAPTCHA v3 site key (public). Leave blank locally — the contact form
# and backend both skip verification gracefully when unset.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
