Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Cors==4.0.1
Flask-Bcrypt==1.0.1
Flask-Limiter==3.8.0
PyJWT==2.9.0
python-dotenv==1.0.1
requests==2.32.3
gunicorn==22.0.0

# MSSQL driver for production (Azure SQL). Requires the msodbcsql18 system
# driver to be installed on the host — see README for setup notes.
pyodbc==5.1.0
